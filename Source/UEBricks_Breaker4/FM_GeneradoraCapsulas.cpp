// Fill out your copyright notice in the Description page of Project Settings.


#include "FM_GeneradoraCapsulas.h"

// Sets default values
AFM_GeneradoraCapsulas::AFM_GeneradoraCapsulas()
{
 	// Set this pawn to call Tick() every frame.  You can turn this off to improve performance if you don't need it.
	PrimaryActorTick.bCanEverTick = true;

}

// Called when the game starts or when spawned
void AFM_GeneradoraCapsulas::BeginPlay()
{
	Super::BeginPlay();
	
}

// Called every frame
void AFM_GeneradoraCapsulas::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);

}

// Called to bind functionality to input
void AFM_GeneradoraCapsulas::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
{
	Super::SetupPlayerInputComponent(PlayerInputComponent);

}

